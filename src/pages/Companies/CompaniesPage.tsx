import CompaniesListPage from './components/CompaniesListPage';

const CompaniesPage = () => {
  // This page now directly shows the companies list
  // The dashboard navigation handles the routing to different sections
  return <CompaniesListPage />;
};

export default CompaniesPage;
