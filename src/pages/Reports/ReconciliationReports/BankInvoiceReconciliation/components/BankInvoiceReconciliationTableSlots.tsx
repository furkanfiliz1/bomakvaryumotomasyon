/**
 * Table slots for Bank Invoice Reconciliation
 * Custom rendering is handled in table headers configuration
 */
export const BankInvoiceReconciliationTableSlots = () => {
  return null; // Table formatting handled by headers configuration
};
