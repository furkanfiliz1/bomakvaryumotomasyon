// Export routes and navigation configuration
export * from './reports.routes';
export * from './reports.nav-config';

// Export main page component
export { default as ReportsPage } from './ReportsPage';

// Note: Report modules are available via direct imports if needed
// Individual components can be imported directly from their component folders
